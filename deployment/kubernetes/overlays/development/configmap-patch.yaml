apiVersion: v1
kind: ConfigMap
metadata:
  name: providers-portal-config
  namespace: lsevin-providers
data:
  PGHOST: "postgres"
  NEXT_PUBLIC_APP_URL: "http://localhost:3000"
  PROVIDER_MEDIA_PUBLIC_BASE_URL: "http://localhost:3000"
  PROVIDER_PORTAL_LOCAL_AUTH: "true"
