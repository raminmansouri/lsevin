apiVersion: apps/v1
kind: Deployment
metadata:
  name: providers-portal
  namespace: lsevin-providers
spec:
  template:
    spec:
      containers:
        - name: web
          env:
            - name: NODE_ENV
              value: development
