# Fail when a new dependency introduces an unreviewed install script.
strictDepBuilds: true

# Dependencies reviewed and permitted to run install/postinstall scripts.
allowBuilds:
  "@tailwindcss/oxide": true
  esbuild: true
  sharp: true
  unrs-resolver: true