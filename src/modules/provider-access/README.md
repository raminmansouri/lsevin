# provider-access module

Owns provider membership, role checks and linking an existing `category.service_providers` record to an existing LSevin user via `provider_portal.provider_members`.

This module is the gatekeeper for all provider-scoped modules.
