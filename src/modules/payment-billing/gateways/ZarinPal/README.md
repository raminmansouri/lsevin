# ZarinPal adapter

Keep gateway credentials, request mapping, verification, refund and webhook logic inside this adapter folder. Business logic must use PaymentBilling contracts, not gateway-specific objects.
