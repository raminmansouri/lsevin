# قوانین حفاظتی — بالاترین اولویت

این سند بر پرامپت اصلی و اصلاحیه ۱ و هر دستور دیگری اولویت دارد. هر جا تعارضی دیدی، این سند برنده است.

**اصل کلی:** تو اینجا باگ رفع می‌کنی، نه معماری بهتر می‌کنی. کد این پروژه در حال سرویس‌دهی است. هر تغییری که برای رفع باگِ مشخصِ اعلام‌شده لازم نباشد، خارج از اختیار توست، حتی اگر مطمئنی بهتر است. اگر چیزی را باید عوض کرد و در لیست نیست، گزارشش کن و منتظر بمان.

---

## ۱. معماری قفل است

این‌ها تصمیم‌های گرفته‌شده پروژه هستند، نه چیزی که در حین کار بازنگری شود:

- SQL خام با postgres.js. هیچ ORM، query builder یا لایه انتزاعی جدیدی اضافه نمی‌شود.
- Server Actions با الگوی موجود: `assertAdmin()` بعد `schema.parse()` بعد کوئری بعد `revalidatePath()`. همین ترتیب، همین شکل.
- ساختار پوشه‌ای فعلی: `src/features/<domain>/{actions,components,api,lib,schemas,types}`. پوشه سطح بالای جدید نساز، قرارداد نام‌گذاری را عوض نکن.
- next-intl برای متن‌ها، shadcn/ui برای کامپوننت‌ها، react-hook-form با zodResolver برای فرم‌ها.
- الگوهای وضعیت و کش فعلی. کتابخانه state management، data fetching یا فرم جدید معرفی نکن.

هیچ‌کدام از این‌ها بدون تایید صریح مالک پروژه تغییر نمی‌کند.

## ۲. افزودنی باش، نه بازنویس

- کامپوننت جدید در فایل جدید بهتر از دست بردن در فایل موجود است.
- فایل موجود را فقط به اندازه اتصال تغییر بده.
- فایل را جابجا یا rename نکن.
- امضای تابع، نام export، شکل props و شکل خروجی کوئری‌هایی که جای دیگری مصرف می‌شوند را عوض نکن. اگر مجبوری، بایست و بپرس.
- کد موجود را برای «تمیزتر شدن» بازنویسی نکن. قالب‌بندی فایل را عوض نکن. `eslint --fix` یا prettier روی فایل‌های نامرتبط اجرا نکن. دیف باید فقط همان چیزی باشد که واقعا عوض شده.
- کد مرده‌ای که پیدا می‌کنی را حذف نکن. وصل کردنش کار درست است، نه پاک کردنش. بقیه موارد را فقط لیست کن.

## ۳. فایل‌هایی که اصلا دست نمی‌خورند

`next.config.ts`، `tsconfig.json`، پیکربندی eslint و prettier، `package.json`، `pnpm-lock.yaml`، `.gitignore`، `middleware.ts`، فایل‌های CI و جنکینز، و هر فایل env.

به‌طور خاص: `ignoreBuildErrors: true` را برنگردان و آن ۷۴۶ خطای موجود را «درست نکن». آن‌ها موضوع این تسک نیستند.

پکیج جدید نصب نکن. اگر برای انجام کاری واقعا پکیج لازم است، بایست و بپرس.

## ۴. دیتابیس

- فقط ستون یا جدول جدید. ستون موجود را rename، drop یا تغییر نوع نده.
- ستون جدید همیشه nullable یا با default باشد، تا کد قدیمی بدون تغییر کار کند.
- هیچ `DROP`، `TRUNCATE`، `DELETE` یا `UPDATE` بدون `WHERE` نمی‌نویسی.
- بک‌فیل داده بدون تایید مالک پروژه انجام نمی‌شود، حتی اگر مایگریشن بدون آن ناقص به نظر برسد.
- روی دیتابیس production هیچ چیزی اجرا نمی‌کنی. فقط فایل SQL تحویل می‌دهی.
- تریگرها، ایندکس‌ها و constraint‌های موجود دست‌نخورده می‌مانند. اگر ایندکس جدید لازم است، در همان فایل مایگریشن با توضیح.

## ۵. گیت

- فقط همان جریانی که تعریف شده: کامیت هر آیتم و پوش روی `Lsevin-New`.
- ممنوع: `push --force`، rebase تاریخچه پوش‌شده، `commit --amend` روی کامیت پوش‌شده، revert کردن کامیت‌های قبلی، ساخت یا حذف برنچ دیگر، تگ زدن، تغییر ریموت.
- `git checkout .`، `git restore`، `git clean` و `git stash drop` اجرا نکن. فایل‌های تغییر یافته محلی به هیچ عنوان پاک نشوند، تا وقتی تکلیفشان روشن شود.
- clone تازه یا حذف و نصب دوباره پروژه، اگر لازم شد، فقط پیشنهاد بده. خودت انجام نده.
- کامیت فقط شامل فایل‌های همان آیتم. `git add -A` ممنوع.

## ۶. حالت اجرا — مجوز صریح

**اصل:** دست بردن در چیزی که هست ممنوع، ساختن چیزی که نیست آزاد.

برای این آیتم‌ها تا آخر برو و وسط کار نپرس: **B1** (اگر UI آپلود نیست، بنویسش؛ اگر `media-picker-adapter` گم است، بسازش)، **B2**، **B3** (ستون `deleted_at timestamptz null`، اکشن حذف از صفر، گارد استفاده، دکمه و مودال — بدون فلگ ارز پایه)، **B5** (اصلاح مسیرهای خواندن به `image_url`؛ اگر فرم آن را پر نمی‌کند آن هم درست شود)، **B6** (مایگریشن `is_featured`، تیک در فرم ادمین، ستون و فیلتر در لیست)، **B9**، **F2** (اول `provider-portal/.../availability` را بخوان، اگر مدل قابل استفاده‌ای هست روی همان سوار کن).

**F1 تنها آیتم بلوکه است** — تصمیم محصولی، منتظر مالک پروژه.

سقف ۱۵۰ خط و ۵ فایل برداشته شد.

**DDL همیشه با `if not exists`،** تا وابستگی به دامپ قدیمی از بین برود:

```sql
alter table category.provider_services
  add column if not exists is_featured boolean not null default false;
```

## ۷. STOP فقط در این سه حالت

1. کاری که می‌خواهی بکنی در فهرست ممنوعه بند ۳ یا ۴ است.
2. تغییر تو رفتار جای دیگری از سیستم را عوض می‌کند — یعنی امضا یا خروجی تابعی که چند جا مصرف می‌شود. صرف اینکه فایلی چند جا استفاده شده مانع نیست؛ ملاک عوض شدن رفتار است.
3. انتخاب بین دو راه، تصمیم محصولی است نه فنی.

خارج از این سه، تصمیم بگیر و جلو برو. تصمیم‌های قابل بحث را در گزارش پایانی زیر عنوان «تصمیم‌هایی که خودم گرفتم» بنویس.

**قالب گزارش STOP:**

```
STOP — آیتم <کد>
چه چیزی پیدا شد:
چرا نمی‌توانم خودم تصمیم بگیرم:
گزینه‌ها و پیامد هرکدام:
پیشنهاد من:
```

## ۸. گزارش

گزارش هر آیتم را وسط کار نده و منتظر جواب نمان. پشت سر هم کار کن و در پایان یک گزارش بده شامل:

- برای هر آیتم: وضعیت (انجام‌شده / ناقص)، فایل‌های تغییر یافته، هش کامیت
- فهرست فایل‌های SQL که باید دستی اجرا شوند، به ترتیب
- تصمیم‌هایی که خودت گرفتی
- خطای جدید tsc نسبت به بیس‌لاین ۷۴۶
- چیزهایی که دیدی و دست نزدی (کد مرده، رشته‌های هاردکد، کوئری بدون `order by`، خطاهای tsc موجود)
